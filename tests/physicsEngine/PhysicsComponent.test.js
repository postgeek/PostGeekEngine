import PhysicsComponent from '../../src/physicsEngine/PhysicsComponent';

describe('Collision', () => {
  it('should return true if there is a collision between the object and the left side fo the world', () => {
    // Arrange
    // Create a hitbox
    // Create the world

    // Act
    // Test collision between objects and world

    // Assert
    // expect(() => { graphicObject.internalDraw(); }).toThrow(MethodNotImplementedError);
  });
});
